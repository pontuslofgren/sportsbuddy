<nav class="navbar navbar-expand-lg sb-navbar">
  <div class="container">
        <a class="navbar-brand text-white" href="/"><%= image_tag "sportsbuddy_logo.png", width: 150 %></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">

        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <%= link_to 'Home', root_path, class:"nav-link #{"bg-primary rounded text-white" if current_page?(root_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to 'Workouts', workouts_path, class:"nav-link #{"bg-primary rounded text-white" if current_page?(workouts_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to 'About us', about_path, class:"nav-link #{"bg-primary rounded text-white" if current_page?(about_path)}" %>
          </li>
        </ul>

        <ul class="navbar-nav">
          <li class="nav-item">
            <% if user_signed_in? %>
              <%= link_to 'Profile', profile_path(current_user.id), class:"text-white btn sb-full-width #{current_page?(profile_path(current_user)) ? "btn-primary" : "btn-outline-primary"}" %>
            <% else %>
              <%= link_to 'Log in', new_user_session_path, class:"text-white btn btn-outline-primary sb-full-width" %>
              <%= link_to 'Sign up', new_user_registration_path, class:"text-white btn btn-outline-primary sb-full-width" %>
            <% end %>
          </li>
        </ul>

      </div>



    </div>
</nav>
